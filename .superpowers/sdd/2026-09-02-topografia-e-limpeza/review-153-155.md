# Review: Task 153 (5360a3b) + Task 155 (cceb980)

## Progresso
- 2026-09-04 00:10 — briefs read (task-153-brief.md, task-155-brief.md), rubric read (docs/papeis/revisor.md)
- 2026-09-04 00:15 — reports read (task-153-report.md, task-155-report.md), review diff read
- 2026-09-04 00:20 — go build ./... clean; go vet on the four packages clean
- 2026-09-04 00:25 — go test -race -count=1 on ipc/daemon/doctor/cmd/gobsidian: all PASS
- 2026-09-04 00:30 — mutation proof redone by hand for both tasks (doctor/daemon.go lock filter; ipc/desconexao.go os.ErrClosed) — both reproduced the FAIL claimed in the reports, both restored byte-for-byte (sha256 match, `git diff --stat` empty)
- 2026-09-04 00:35 — import graph checked against CLAUDE.md (`go list -f '{{.Imports}}'`): matches exactly, no new edges; gofmt clean on all changed files
- 2026-09-04 00:40 — doc-staleness check: found `docs/wiki/flows/encerramento.md:91` now stale against Task 155's behavior change; verdict written

## Verification performed independently

- `go build ./...` — clean, no output.
- `go vet ./internal/ipc/ ./internal/daemon/ ./internal/doctor/ ./cmd/gobsidian/` — clean, no output.
- `go test -race -count=1 ./internal/ipc/ ./internal/daemon/ ./internal/doctor/ ./cmd/gobsidian/` — all four `ok`.
- Mutation proof, Task 153 (`internal/doctor/daemon.go`, anchor `!daemon.EhArquivoDeTrava(e.Name())` → the brief's fallback `len`-based replacement): test `TestCheckLocksDeDaemonEnxergaListenLock` failed with the exact message the report pasted (`daemon_test.go:131: com a trava de escuta tomada, doctor disse: "nenhuma trava em uso"`). File restored — sha256 identical before/after, `git diff --stat` empty.
- Mutation proof, Task 155 (`internal/ipc/desconexao.go`, `os.ErrClosed` → `os.ErrExist`): test `TestEhDesconexaoLimpaReconheceAsQuatroFormas` failed at `desconexao_test.go:17` on both `os.ErrClosed` cases, matching the report exactly. File restored — sha256 identical, `git diff --stat` empty.
- `go list -f '{{.Imports}}'` for `ipc`, `daemon`, `doctor`, `cmd/gobsidian` — matches CLAUDE.md's graph exactly (`ipc→config`; `doctor→config,daemon,ipc,vault`; `daemon→config,ipc,mcpsrv`); no new edges in either commit.
- `gofmt -l` on all ten changed files — clean.
- Confirmed `strings` stays used elsewhere in `internal/doctor/daemon.go` (b.WriteString etc.), `errors`/`io` stay used elsewhere in `cmd/gobsidian/serve.go`, and `ipc` was already imported by `internal/daemon/daemon.go` before Task 155 — so the import-pruning claims in both reports hold.
- Did not run `scripts/test_orphans.ps1` (per instructions; orchestrator's run is in the report, four `[OK]`, appended 2026-09-04 00:30).

## Findings

- `docs/wiki/flows/encerramento.md:91` — non-blocking — the "Código de saída" section states `shutdownExitCode` treats exactly `ctx.Canceled`, `io.EOF`, and `io.ErrClosedPipe` as clean shutdown (0). This is now wrong: since cceb980, `shutdownExitCode` delegates to `ipc.EhDesconexaoLimpa`, which also treats `os.ErrClosed` as clean — the whole point of Task 155's fix. The page carries `status: active` (not `stale`), lists `cmd/gobsidian/serve.go` in `source_paths`, and `source_commit: c6804e1e` predates this change — so it is exactly the case the orchestrator asked to flag ("a doc that actually describes the changed behavior and is now wrong"). Fix: add `os.ErrClosed` to the sentence (or point at `ipc.EhDesconexaoLimpa` the way the code comments now do) and bump `source_commit`/`updated_at`. This does not invalidate the `[sem-doc]` hatch for the *contract* claim (TOOLS.md/schema/architecture didn't change), but it is a real doc that now misdescribes behavior, which the rubric's "contrato que mente" section treats as worth reporting even outside a formal contract doc.

No other findings. No `net/*` violations, no MCP SDK type leakage, no stdout writes, no `if runtime.GOOS`, no `helpers.go`-style files, ASCII console markers unaffected (neither commit touches console output). Both mutation proofs are genuine (concrete diffs, failing test named and quoted, restore confirmed independently by hash — not just by the report's own say-so). The report's honest admission that `mutate.ps1`'s literal brief command gives `EXIT=2` on both anchors (because the mutation would leave an import unused) checks out — I hit the identical build-break reasoning would apply, and used the brief's own documented fallback for Task 153, and the report's manual fallback for Task 155, both confirmed for real.

Task 155's report is transparent about a real gap: no automated test exercises the actual line in `daemon.go` (`if !ipc.EhDesconexaoLimpa(err) { d.log.Warn(...) }`) — coverage there is by reading, not by test. The report says this plainly rather than papering over it. Not a blocking defect (the function is trivial delegation, and `EhDesconexaoLimpa` itself is well-tested), but worth the team knowing it's read-verified, not test-verified.

## Verdicts

### Task 153 (5360a3b)

**Spec compliance: APPROVED**
- `EhArquivoDeTrava` exported in `internal/daemon/lock.go`, recognizes both suffixes via `sufixoTrava`/`sufixoTravaDeEscuta` — done.
- `lockPath` and `ComLockDeEscuta` consume the constants instead of literal strings — done.
- `doctor/daemon.go:203` (now different line) uses `daemon.EhArquivoDeTrava` instead of the `.sock.lock`-only filter — done.
- `checkDaemonLog` uses `daemon.CaminhoDoLog` instead of `sock + ".log"` — done.
- Tests added in both packages per the brief's literal test code — done, both present and passing.
- `ipc` import retained in `doctor` (edge didn't disappear) — confirmed independently; CLAUDE.md correctly left untouched.
- Mutation proof for the claimed rule — confirmed independently, genuine.

**Code quality: APPROVED**
- Single account for both lock suffixes, consumed by both producer (`lock.go`) and consumer (`doctor`) — matches "uma conta por regra."
- No stray literals left: `grep` for `.lock`/`.listen.lock`/`.log` in both packages only hits the constants (confirmed by re-running the report's grep, matches its output).

### Task 155 (cceb980)

**Spec compliance: APPROVED**
- `internal/ipc/desconexao.go` created with `EhDesconexaoLimpa` recognizing all four forms (nil, `context.Canceled`, `io.EOF`, `io.ErrClosedPipe`, `os.ErrClosed`) — done, matches brief's Step 2 verbatim.
- All three consumers (`serve.go`, `ponte.go`, `daemon.go`) delegate to `ipc.EhDesconexaoLimpa`; the literal `io.ErrClosedPipe` list is gone from all three (`grep -rn "io.ErrClosedPipe" cmd internal` only hits `internal/ipc/desconexao.go` and the two test files — reran independently, matches).
- New test cases (`os.ErrClosed` → 0, `context.DeadlineExceeded` → 1) present in `serve_test.go`.
- No new import edges: confirmed independently via `go list`.
- Orphans gate: four `[OK]` (run by orchestrator per the standing ruling, appended to the report) — not re-run by me, per instructions.

**Code quality: APPROVED**
- Single account (`ipc.EhDesconexaoLimpa`) genuinely replaces three separate hand-rolled lists; comments at each call site point at it instead of re-explaining.
- Unused imports (`errors`, `io`, `os`, `strings` as applicable) correctly pruned in each modified file — confirmed by build+vet clean and by direct inspection.
- The one non-blocking finding above (stale wiki paragraph) is a documentation debt, not a code-quality defect in the commits themselves.

## Findings summary (blocking: none)
- `docs/wiki/flows/encerramento.md:91` — non-blocking — stale: still says shutdownExitCode's clean-shutdown set is three forms, missing `os.ErrClosed` added by cceb980 — fix: mention `ipc.EhDesconexaoLimpa`/add `os.ErrClosed`, bump `source_commit`.
