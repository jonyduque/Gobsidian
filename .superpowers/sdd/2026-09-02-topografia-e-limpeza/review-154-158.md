# Review: Tasks 154, 157, 158, 156 (+ b354dcf doc follow-up)

Base: `cceb980`. Commits reviewed: `7f2ac8d` (154), `b1f2cfe` (157), `3a1ab3e` (158),
`b8ed7f6` (156), `b354dcf` (doc follow-up).

## Progresso
- 01:26 briefs 154/157/158/156 lidos; reports 154/157/158/156 lidos; review-cceb980..b354dcf.diff (804 linhas) e docs/papeis/revisor.md lidos
- 01:26 verificado doctor.go sem import orfao de `fmt` apos delecao de Marker; graph.go mantem `fmt` em uso (3 outros sitios); config.Flags (ReadOnlySet/DebounceMSSet/MaxResultsSet) confirmados em uso por serve.go/daemon.go/doctor.go — nao afetados pela Task 156
- 01:26 `go build ./...` limpo; `go vet ./...` limpo
- 01:27 `go test -race -count=1 ./internal/search/ ./internal/service/ ./internal/doctor/ ./cmd/gobsidian/ ./internal/config/` — 5 pacotes, todos `ok`
- 01:28 `pwsh -File scripts/check_readme_anchors.ps1` — `[OK] toda ancora resolve`; `grep -n 'Flags()' cmd/gobsidian/*.go` colhido para o cruzamento com a tabela README
- 01:28 config_test.go:85-101 lido — confirma que a regra de `TestIndexCmd_DebounceMSFlagZeroRejected` (deletado na Task 156) sobrevive em `internal/config/config_test.go` ("debounce zero rejected from flag", linha 100-104)
- 01:28 `go run ./cmd/gobsidian {search,index,inspect} --help` rodado — flags batem com o relatorio da Task 156
- 01:28 `internal/ipc/desconexao.go` lido — `EhDesconexaoLimpa` cobre exatamente nil, `context.Canceled`, `io.EOF`, `io.ErrClosedPipe`, `os.ErrClosed`; frase reescrita em `docs/wiki/flows/encerramento.md` confere; `source_commit: cceb980` existe no repo
- 01:28-01:29 mutacao manual independente da Task 158: `LinkGraph` (`graph.go:87`) trocado por `fmt.Errorf("indice indisponivel")`; `TestServicoSemIndiceDevolveVaultUnavailable` reprova nomeando `LinkGraph`; restaurado, `git diff --stat` vazio, teste volta a passar
- 01:29 mutacao manual independente da Task 157: bump `CacheFormatVersion` 6→7 em `persist.go` (com o codec ATUAL, pos-alias) — `TestVersaoDoCacheDeBuscaEUmaConta` PASSA (deriva); troca do `persist_codec.go` pela versao PRE-157 (`git show b1f2cfe~1:...`), mesmo bump — o teste REPROVA (`cacheCodecVers = 6, CacheFormatVersion = 7: duas contas`); ambos os arquivos restaurados, `git diff --stat` vazio, bateria `Cache|Persist` (20 testes) verde
- 01:30 `search --json --limit 3` reproduzido num cofre de 5 notas: `results` com 3 itens, `total: 5`, `truncated: true` — `--limit` intacto
- 01:30 `pwsh -File scripts/mutate.ps1` da Task 156 (`MaxResults: cfg.MaxResults` → `MaxResults: 0`) reproduzido — `EXIT=0`, restaurado byte a byte (SHA-256 confere)
- 01:30 `git status --porcelain` sobre os quatro arquivos tocados pelas mutacoes manuais — vazio, nenhum residuo
- 01:30-01:31 cruzamento da tabela README (coluna Subcommands) contra `grep -n 'Flags()' cmd/gobsidian/*.go` — achado bloqueante (ver abaixo)
- 01:31 `golangci-lint run ./internal/doctor/... ./internal/search/... ./internal/service/... ./cmd/gobsidian/... ./internal/config/...` — `0 issues.`
- 01:31 checado `docs/wiki/entities/formato-do-cache.md` (achado nao-bloqueante, ver abaixo) e `docs/ARMADILHAS.md`/`docs/SUGESTOES.md` (mencoes historicas de GBS6/CacheFormatVersion, corretas como historia, sem acao)

---

## Task 154 — drop `doctor.Status.Marker`

**Conformidade com o brief: APROVADO.** Deleção exata dos dois sítios que o brief
pede (`doctor.go:25-35`, `doctor_extra_test.go:20-42`); nenhum chamador de
produção existia (grep colado no relatório, reproduzido: `internal/console`
segue sendo quem imprime os marcadores ASCII, delecao nao o tocou). Ruling do
orquestrador (delecao sem prova de mutacao, prova = build/vet limpos)
respeitada. `grep -rn "Marker(" internal cmd` vazio, confirmado.

**Qualidade: APROVADO.** Sem import órfão (`fmt` nem é importado em
`internal/doctor/doctor.go`, e não precisava ser). `go vet`, `golangci-lint`
limpos, verificado de novo nesta revisão.

Nenhum achado.

## Task 157 — cache format version é uma conta só

**Conformidade com o brief: APROVADO.** Todos os passos batem: teste escrito
conforme o brief (Step 1); bump manual ANTES do alias reproduz o FAIL nas duas
asserções (Step 2, reproduzido de forma independente nesta revisão trocando
`persist_codec.go` pela versão pré-157 via `git show b1f2cfe~1:...` + bump —
mesmo resultado); alias implementado exatamente como o Step 3 pede
(`cacheCodecVers = CacheFormatVersion`, `cacheMagic` vira `var` com
`fmt.Sprintf`); bump DEPOIS do alias faz tudo derivar e a bateria
`Cache|Persist` (20 testes) passa (Step 4, reproduzido); `grep -rn "GBS6\|cacheCodecVers = 6" internal/search` vazio, confirmado.

**Qualidade: APROVADO.** O comentário no código documenta o defeito original
sem deixar deliberação ("Wait,", "for the sake of") — é explicação factual do
porquê, no estilo do resto do projeto. `go vet`, `golangci-lint` limpos.

**Achado não-bloqueante:**
`docs/wiki/entities/formato-do-cache.md:94-104` — não medido como "errado", mas
**desatualizado por omissão**. A seção "O outro cache, e por que a versão dele é
um alias" (linha 94) apresenta o alias `indexCacheCodecVers = IndexCacheFormatVersion`
como uma característica do cache de **metadados**, implicitamente contrastando
com o cache de **busca** (o assunto desta página, `source_paths` inclui
`persist_codec.go`/`persist.go` — exatamente os arquivos que a Task 157 mudou).
Depois de b1f2cfe o cache de busca usa o **mesmo** padrão de alias, e a página
não diz isso; `source_commit: f7de8e81` e `updated_at: '2026-08-31'` também
antecedem o commit. A página segue `status: active`, não `stale`. Não bloqueia
esta revisão (a ata do orquestrador aceitou `[sem-doc]` para 157 porque nenhum
contrato normativo — `TOOLS.md`, `PRD.md` — mudou, e o formato on-disk/magic
bytes são idênticos); fica como item para a próxima passada de manutenção do
wiki. **Fix sugerido:** marcar `status: stale` ou atualizar a seção para
mencionar que ambos os caches agora usam o mesmo padrão de alias.

## Task 158 — serviço sem índice devolve VAULT_UNAVAILABLE

**Conformidade com o brief: APROVADO**, com os desvios já aprovados pela ata do
orquestrador verificados nesta revisão:
- Código tinha QUATRO sítios em `graph.go`, não cinco — confirmado
  (`LinkGraph:87`, `TagList:303`, `ListNotes:455`, `NoteMetadata:556`).
- `VaultStats` não foi tocado — confirmado no diff (nenhuma mudança em torno de
  `~662`); ela segue com o fallback `vault.Walk` e não retorna erro.
- Linha "Stats" fora da tabela do teste — confirmado em `errors_test.go`, o
  mapa `chamadas` tem só quatro entradas.
- Método real é `ListNotes` (não `NoteList`), tipo `ListRequest` — usado
  corretamente no teste.
- `outline.go:71` migrado de `CodeInternal` para `CodeVaultUnavailable`,
  conforme pedido; `docs/TOOLS.md` atualizado nos dois lugares (linha de
  `VAULT_UNAVAILABLE` na tabela de erros, e a seção "Erros" de `note_outline`)
  — texto lido, cobre "índice indisponível" e a leitura de nota falha, e não
  promete mais `INTERNAL`.

Mutação: âncora ambígua nos quatro sítios idênticos, mutação manual em UM sítio
(`LinkGraph`) conforme o brief previa para esse caso. **Reproduzida de forma
independente nesta revisão**: mutado, teste reprova nomeando `LinkGraph` e a
linha da asserção (`errors_test.go:52`), restaurado, `git diff --stat` vazio.

**Qualidade: APROVADO.** `fmt` continua em uso em `graph.go` (três outros
sítios: `chaveDaAresta`, dois `Hash: fmt.Sprintf(...)`), nenhum import órfão.
`go vet`, `golangci-lint`, `go test -race` (reproduzido: `ok` em 55.7s)
limpos.

Nenhum achado.

## Task 156 — CLI `search` respeita `--max-results`; `index`/`inspect` param de flags ignoradas

**Conformidade com o brief: NÃO APROVADO** — um achado bloqueante na tabela do
README (abaixo). O resto do escopo confere:
- `search.go`: `ReadOnlySet`/`DebounceMSSet` removidos, `MaxResultsSet` mantido;
  `service.Options{..., MaxResults: cfg.MaxResults}` chega ao serviço —
  confirmado pelo teste e pela mutação (reproduzida nesta revisão, `EXIT=0`,
  restaurado byte a byte).
- `index.go`/`inspect.go`: as três linhas `flags.*Set` e as três declarações de
  flag removidas — confirmado no diff e no `--help` (reproduzido nesta
  revisão: nenhuma das três aparece).
- `TestSubcommands_FlagsSetPopulated` e `TestIndexCmd_DebounceMSFlagZeroRejected`
  deletados; a regra do segundo (`--debounce-ms=0` rejeitado) sobrevive em
  `internal/config/config_test.go:100-104` ("debounce zero rejected from
  flag") — confirmado por leitura direta do arquivo, a ruling do despacho se
  sustenta.
- `daemon`/`doctor` continuam declarando as três flags que ignoram — fora de
  escopo por ruling, não é achado desta tarefa em si (mas ver abaixo: isso
  vaza para a tabela do README, que é escopo desta tarefa).
- `--limit` continua funcionando — reproduzido nesta revisão (`--limit 3` sobre
  5 notas: 3 resultados, `total: 5`, `truncated: true`).
- `check_readme_anchors.ps1` verde — reproduzido.

**Achado bloqueante:**
`README.md:58-69` (coluna **Subcommands** da tabela de flags) — **a tabela
está incompleta/incorreta para seis das dez linhas**, cruzando contra
`grep -n 'Flags()' cmd/gobsidian/*.go`:

| Flag | Tabela diz | Código realmente declara |
|---|---|---|
| `--read-only` | `serve` | `serve`, `daemon` (`daemon.go:57`), `doctor` (`doctor.go:68`) |
| `--cache-dir` | `serve` | `serve`, `daemon` (`daemon.go:60`) |
| `--debounce-ms` | `serve` | `serve`, `daemon` (`daemon.go:58`), `doctor` (`doctor.go:69`) |
| `--log-level` | `serve` | `serve`, `daemon` (`daemon.go:56`) |
| `--eager-search` | `serve` | `serve`, `daemon` (`daemon.go:63`) |
| `--max-results` | `serve`, `search` | `serve`, `search`, `daemon` (`daemon.go:59`), `doctor` (`doctor.go:70`) |

(`--vault`, `--follow-symlinks`, `--json`, `--limit` conferem exatamente.)

O próprio brief (Step 5) prescreveu os valores errados ("`--read-only`,
`--debounce-ms`, `--cache-dir`, `--eager-search`, `--log-level`: `serve`") —
não é um desvio do implementador, é um defeito herdado do brief. Mas o mesmo
Step 5 também instruiu: "Confira cada flag contra o código antes de escrever a
linha — a tabela é contrato", e a verificação #1 do relatório da Task 156
afirma "cada flag conferida contra `grep -n 'Flags()' cmd/gobsidian/*.go` antes
de escrever a linha" — essa conferência, se tivesse incluído `daemon.go` e
`doctor.go` (que o próprio `grep` sugerido lista, porque o glob é
`cmd/gobsidian/*.go`, não só os três arquivos tocados), teria pego a
divergência. A ruling do despacho isenta a tarefa de **consertar**
`daemon`/`doctor` (eles continuam declarando flags que ignoram, por decisão de
escopo), mas não isenta a **tabela** de descrever com precisão o que o código
já faz — e "a tabela é contrato" é frase do próprio brief. Uma tabela que diz
"`--read-only`: `serve`" quando `daemon` e `doctor` também aceitam (e,
adicionalmente, ignoram) essa flag é um contrato que mente por omissão,
análogo ao que a regra "Schema que promete e código que ignora é pior que
parâmetro ausente" cobre no sentido inverso — aqui é o código que faz mais do
que a tabela promete descrever.

**Fix:** ou (a) estender a coluna Subcommands para incluir `daemon`/`doctor`
nas seis linhas acima (`serve, daemon, doctor` etc.), ou (b) adicionar uma nota
explícita na tabela dizendo que ela cobre só `serve`/`search`/`index`/`inspect`
e que `daemon`/`doctor` têm uma superfície de flags própria e não auditada
ainda (referenciando a dívida deixada em "O que ficou de fora" do relatório).
A opção (a) é mais barata e não expande escopo — é ler o `grep` que o próprio
brief já pede.

**Qualidade: APROVADO** (independente do achado acima, que é de conformidade/
conteúdo, não de qualidade de código). `go vet`, `golangci-lint`, `go test -race`
limpos; sem import órfão; commits segregados por categoria (o follow-up de doc
foi corretamente separado em `b354dcf`, por instrução do despacho).

## b354dcf — doc follow-up (`docs/wiki/flows/encerramento.md`)

**Verdito: APROVADO.** A frase reescrita da seção "Código de saída" corresponde
exatamente a `internal/ipc/desconexao.go`: `EhDesconexaoLimpa` cobre `nil`,
`context.Canceled`, `io.EOF`, `io.ErrClosedPipe`, `os.ErrClosed` — a prosa lista
as quatro formas de erro (`nil` não é "forma de erro", é ausência dele, e a
prosa corretamente não o lista como uma das "quatro formas no retorno do serve
loop"). Frontmatter: `source_commit: cceb980` (existe no repo, é o commit-base
deste lote, que introduziu `EhDesconexaoLimpa`) e `updated_at: '2026-09-04'`
presentes — bump confirmado. Encoding não verificado por mim diretamente (não
há sinal de mojibake na leitura), mas o relatório da Task 154 registra a
validação UTF-8 rodada.

---

## Resumo de verificação independente

- `go build ./...` — limpo
- `go vet ./...` — limpo
- `go test -race -count=1 ./internal/search/ ./internal/service/ ./internal/doctor/ ./cmd/gobsidian/ ./internal/config/` — 5/5 `ok`
- `golangci-lint run` nos cinco pacotes — `0 issues.`
- `pwsh -File scripts/check_readme_anchors.ps1` — `[OK]`
- Mutação 158 (`LinkGraph`) — reproduzida, `FAIL` nomeando o método e a linha, restaurado
- Mutação 157 (bump `CacheFormatVersion`, pré e pós-alias) — reproduzida nas duas direções, restaurado, bateria `Cache|Persist` (20 testes) verde
- Mutação 156 (`mutate.ps1`, `MaxResults`) — reproduzida, `EXIT=0`, restaurado byte a byte (SHA-256)
- `search --json --limit 3` — reproduzido, `--limit` intacto
- `git status --porcelain` sobre os quatro arquivos tocados por mutações manuais desta revisão — vazio

## Veredito consolidado

| Task | Conformidade | Qualidade |
|---|---|---|
| 154 | APROVADO | APROVADO |
| 157 | APROVADO | APROVADO |
| 158 | APROVADO | APROVADO |
| 156 | **NÃO APROVADO** | APROVADO |
| b354dcf | APROVADO | — |

**Achado bloqueante único:** `README.md:58-69` — coluna Subcommands incorreta
para `--read-only`, `--cache-dir`, `--debounce-ms`, `--log-level`,
`--eager-search`, `--max-results` (faltam `daemon`/`doctor` nas seis linhas,
apesar de o código as declarar). Fix: estender a coluna com os subcomandos
reais, por `grep -n 'Flags()' cmd/gobsidian/*.go` — o mesmo comando que o brief
já manda rodar.
