# Fechamento do plano 2026-09-02-topografia-e-limpeza

Intervalo: `6c5d1f1..b3ee480` (código) + `bfe968d` (artefatos). 36 Tasks
(146–181), revisão final de branch (Opus, `review-final.md`), uma leva de
correção (`cd61624`, `ada4897`, `b3ee480`) e re-revisão aprovada
(`review-final-round1.md`). Gate `verify.ps1` completo verde nas três
correções finais (saída em `task-final-report.md`). Gate de órfãos: rodado
nas Tasks que tocaram lifecycle/daemon/cmd (159, 174, 174b, 177 — logs
`orphans-*.log.err` no workspace); NÃO rodado na leva final (ruling:
`doctor` está fora do caminho serve/daemon).

## Todas as rulings, na ordem do ledger

Extraídas de `.superpowers/sdd/2026-07-25-gobsidian-v01/progress.md`
(a partir de "Task 146: complete"), verbatim.

7. Ruling: o teste do watcher foi para `internal/watcher/varredura_raiz_ilegivel_test.go`
11. Ruling: a prova de mutação no ponto de chamada do watcher não executa em
20. Ruling: re-revisão dispensada para a74b766 — diff de 7 linhas de comentário,
27. Task 149 — Ruling: a verificação 2 do brief cita "a tabela de erros de
34. Task 150 — Ruling: o `mutate.ps1` do brief acrescenta `"xyz"` à lista de
44. Ruling (2026-09-03, owner: "Pode agrupar tasks pequenas"): batches of small, file-disjoint tasks go to ONE implementer dispatch and ONE review, keeping one commit per task as the plan mandates. Batches: [153+155] (daemon/lock.go vs daemon/daemon.go, disjoint); [154+156+157+158]; [160+163]; [162+164]; [166+167+168] (166/167 both touch mcpsrv/server.go, same agent edits sequentially); [178+179]. Alone: 151, 152 (write.go after 151), 159, 161 (overlaps 162/163 in bm25_test/persist_test), 165, 169, 170, 171-177, 180, 181. Batch model = highest tier of its members. — why: verify.ps1 dominates task wall-clock (~12 min/task measured on 148/149); a batch runs it once per dispatch. — cost if wrong: a fix round on one member re-reviews the batch diff; capped by keeping batches at <=4 tasks.
46. Ruling (2026-09-03, owner: "utilize no maximo 3 agentes em paralelo"): cap honoured; effective concurrency stays 1 (implementer XOR reviewer) — why: all agents share one working tree; a reviewer's mutate.ps1 or verify.ps1 running while an implementer edits the same tree produces false reds or mutates a file mid-edit. Parallelism comes from batching, not from concurrent agents. — cost if wrong: wall-clock only.
48. Ruling (2026-09-03): every dispatch carries "run the gate in the foreground; run_in_background/Monitor never wake a subagent here" — why: impl-150 and rev-150 both parked on dead background tasks (18h and ~7 min lost). — cost if wrong: none.
61. Task 151: complete (commit 9fb5500, review clean — Opus). Parked, non-blocking: (1) write.go:712 existence Stat now runs outside the lock in the trash branch — a concurrent delete loser gets INTERNAL instead of NOTE_NOT_FOUND (fix: map IsNotExist in moverCorpo's fallback ReadFile); (2) destinoNaLixeira: `err` from os.Stat shadows the named return inside the collision block — correct today (checked on the next line), lint-clean; (3) moverCorpo would self-deadlock on de == para; MoveNote guards via CodeNoteExists, trash guards via .trash being IsExcludedDir. Ruling: no fix round — why: none changes behaviour on any reachable path; a gate+re-review costs ~15 min for zero product change. — cost if wrong: (1) a misleading error code under a race nobody has hit.
63. Task 152: complete (commit a637b7f, review clean — Sonnet). Rulings: tests via createMoveService (package service_test); TOOLS.md edit on the note_move Retorno sentence; mutation anchor = one-line referrer error return -> `continue`.
65. Rulings for batch [154+156+157+158] (pre-checked against code 2026-09-03, before dispatch):
66. - Ruling: 154 is 53 lines (check_briefs CURTO) — accepted as-is; deletion task, no mutation proof — nothing to mutate.
67. - Ruling: 156 — no `escreve` helper exists in cmd/gobsidian tests; use `os.WriteFile`. `TestSubcommands_FlagsSetPopulated` and `TestIndexCmd_DebounceMSFlagZeroRejected` (cli_subcommands_test.go:168,:197) exercise the flags the task removes — delete both; debounce-zero rejection and flag-over-env are covered in internal/config/config_test.go:85-101 — why: a test of a flag that no longer exists cannot compile — cost if wrong: none, config tests own the rule.
68. - Ruling: 156 README table at README.md:194-208 only lists `serve` options; add a "Subcommands" column per brief, verifying each flag against `cmd/gobsidian/*.go`; `--max-results` is declared by serve, daemon, doctor, index, inspect, search today — after the task: serve, daemon, doctor, search (doctor/daemon out of scope, leave them). `--limit` (search, default 20) stays alongside `--max-results`.
69. - Ruling: 158 — brief says five `fmt.Errorf("index not available")` sites; code has FOUR (graph.go:87 LinkGraph, :303 TagList, :455 ListNotes, :556 NoteMetadata). VaultStats:663 has `if s.index == nil` with a vault.Walk fallback, not an error — leave it, drop the "Stats" case from the table test (or assert success). Method is `ListNotes`, not `NoteList`. Scripted mutation anchor is ambiguous across four identical lines → manual single-site mutation per brief fallback — why: brief written from prose, not from code — cost if wrong: one test row.
73. Ruling: an "idle" row in ListAgents does not mean the agent has no gate in flight — check Win32_Process for pwsh test_orphans/verify before dispatching a replacement — why: replacing an agent whose gate is still running puts two writers on one tree — cost if wrong: one wasted dispatch.
74. Ruling (2026-09-03, owner request): every dispatch requires a `## Progresso` section in the report file, one timestamped line appended at the end of each step before the next starts — why: agents that die mid-task left no trace of where they stopped; the orchestrator must be able to resume from the file — cost if wrong: a few lines of noise per report.
75. Ruling: implementers do NOT run scripts/test_orphans.ps1. Measured 2026-09-03: daemon-idle ≈ 13 s/cycle → ~22 min for that scenario alone, ~40 min for all four; the harness Bash cap is 10 min, so the call always times out and the agent goes idle mid-gate. The orchestrator runs the gate detached (Start-Process, stdout to a file) after the commit and before the review dispatch, and appends the result to the report — why: the gate exceeds any timeout an agent can set — cost if wrong: gate result lands in the report by a second hand.
76. Ruling: orchestrator polls liveness actively — ListAgents state + report mtime + gate processes — every ≤10 min; an idle agent without DONE gets one nudge, then a replacement.
79. Ruling: progress-log timestamps must come from `Get-Date -Format HH:mm` / `date`, never typed — rev-153-155 wrote 00:10→00:40 for a run that took ~3 min — why: an invented timeline is an unmeasured number — cost if wrong: none.
82. Rulings for Task 159 (pre-checked 2026-09-04):
83. - Ruling: `boundedWait` differs today (daemon 5 s, ipc 3 s, serve 2 s); `vaulttest.Prazo = 5 s` replaces all three — a longer bound only lengthens a FAILING test — cost if wrong: slower red runs. `ponte_test.go` and `ipc/maxresults_handshake_test.go` also use boundedWait and must migrate too.
84. - Ruling: `search/cloudonly_update_windows_test.go:30` has a second `marcarSomenteNuvem` not listed in the brief — migrate it as well (six helpers become seven).
85. - Ruling: the dir-lock helper lives in `internal/vault/walk_raiz_windows_test.go:19` (package `vault`, internal), not walk_windows_test.go. An internal test of `vault` cannot import `vaulttest` (cycle). The file uses only exported identifiers (`New`, `Entry`, `v.Walk`) → convert it to `package vault_test` and migrate; if conversion proves impossible, keep the local helper with a comment pointing to vaulttest and say so in the report.
86. - Ruling: docs/papeis/testador.md has no "handle exclusivo" section today — add a short one (where Windows-specific tests are discussed) pointing to `internal/vaulttest`. docs/ESTRUTURA.md:229-230 shows a stale graph (mcpsrv → service → …) — out of scope for 159; noted for the final message.
94. Task 159 review (rev-159, Opus, review-159.md): Spec APROVADO, Quality APROVADO, 0 blocking, 9 non-blocking. Adjudication: (1) ipc_test.go:64/:235 use Prazo as a latency ceiling, not a wait — FIX with a local ceiling; (2) D5 under-reported — covered by (1); (3) "cinco cópias / só uma conferia" in testador.md:142 and vaulttest/doc.go:8 — reviewer measured 8 sites / 4 proving — FIX both texts; (4) abrirExclusivo Skips where two old copies Fatal'd — Ruling: keep the Skip, it names its reason; two tests trade a named failure for a named skip only when CreateFile itself fails — cost if wrong: an environment that cannot lock goes unnoticed as a skip; (5) build_descarte_unix_test.go chmod 0000 proves nothing as root — FIX with the symmetric ReadFile proof; (6) move_atomico guard message blames the fixture when err==nil — FIX by adding err to the message; (7) CLAUDE.md go list claim holds only on GOOS=windows — FIX wording; (8) CLAUDE.md tree lacks vaulttest — FIX one line; (9) "14 etapas" — Task 163 adds the SKIP-count step, which closes it. Fix round 1 for 159 is DEFERRED until impl-160-163 commits: two writers on one tree already cost a contaminated gate once, and 163's measurements must not overlap a verify.ps1 run — why: sequencing beats speed here — cost if wrong: ~1 h later close-out of 159, nothing downstream depends on these fixes.
95. Batch [160+163] dispatched to impl-160-163 (Opus) at 02:27, base 5377559. Rulings: 160 Step 1 mutation targets internal/vault/walk.go:166 (overflow.go has no ctx check); 160 Step 4 sites are :169/:202/:235 (`data`, not `got`); :101/:128 left alone (they already assert on len); 163 corpus at %TEMP%\vault_5000 kept if 5000 notes, else regenerated Seed 42; ceilings from PRD.md:288-294 with the existing raceEnabled const; the SKIP-count step derives from the existing go test output (no second pass) and makes verify.ps1 14 steps, CLAUDE.md sentence updated; measurements only after the detached orphans gate (PID 44884) exits.
96. Orphans gate for 159 (detached, 02:23→02:44): stdin-eof [OK] 100/100, parent-death [OK] 100/100; signal and daemon-idle REFUSED by the gate's staleness check ("bin\gobsidian.exe e mais antigo que schema_params_test.go") because impl-160-163 was editing that file mid-run. Ruling: accepted for 159 — c81b2b8 changes no file compiled into gobsidian.exe (vaulttest is imported only by _test.go; go list confirms no production importer), so the gate was insurance, not evidence; the full four-scenario gate re-runs after the last task that touches production code and before the final review — why: a gate that cannot see the change proves nothing about it either way — cost if wrong: an orphan regression from 160–164 (all test-only) would surface only at the end-of-plan run.
104. - Ruling: N3 (`getVault5000Path` still Skips when `%TEMP%\vault_5000` is absent) stays as is — the vault is environment, and verify.ps1 step 3 now counts the skip out loud; the report gets one line saying so — cost if wrong: a machine without the vault stays green-without-measurement, but visibly.
105. - Ruling: N8 (Logf of `rebuildDur/loadDur` ratio) not applied — Task 163's own point was budgets replacing Logf; the assertion is the guard — cost if wrong: a 5% inversion is indistinguishable from a 6x one until it flips.
112. - Ruling: O2 gets fixed in round 2 (Compact only on the tags comparison, the alternative the original N4 offered) — a comparison that stops counting is a weaker guard than the one the task replaced — cost if wrong: none beyond one more edit.
113. - Ruling: Task 163 fix round 2 is QUEUED until `impl-161` reports — 161 does hand mutations across service/search/index and a concurrent verify.ps1 from either writer would fail spuriously; one writer on the tree.
117. - Ruling: 161 polish round (queued behind 163 round 2, one writer): rename `TestPoolReuse`; one sentence in the report declaring the proof covered vault_search only (the brief's unit is the site, and the site got its proof); bm25:251 asserts only what the idf rule requires — cost if wrong: one more small diff. config_test.go silent return parked for the out-of-scope list; the 501-file cost noted.
120. - 2026-09-06 00:15 — Task 161 polish round `6c1f217` (rename `TestNormalizeAcentosECaixa`, bm25 ordering line removed with the derived-not-measured 1.507/1.457 note, report sentence on proof scope). Ruling: re-reviewed by the orchestrator — a two-file diff of a rename, a comment and one removed assertion, idf mutation FAIL pasted, verify green in the report — cost if wrong: a missed nit in a comment. Task 161 commits now b3bcba0, ebc0f12, 4ffaf50, 6c1f217.
124. - Ruling: N3 applied — filler becomes `20 + ((i*7+29)%60) + i/60` (the reviewer's phase shift), six tsv regenerated. Deviates from the brief's verbatim formula because that formula recreates the "shortest note wins" confounder the task exists to remove — cost if wrong: one testdata regeneration.
125. - Ruling: B1's commit message cannot be rewritten (no amend of a landed commit); a correcting paragraph in the report plus the two docstrings in the fix commit is the remedy.
130.   Ruling: N2 corrected in the report, not by amending af93306 — same as 162/d12f84f; history stays linear — cost: one wrong count in git log.
131.   Ruling: N4 apply, delete TestCounters_Reconciliations — same rule Task 161 used for redundant tests; overflow_test asserts the exact value and the non-overflow control — cost: a counter test lives one file away from its name.
132.   Ruling: N1, N3, N5 apply. Fix round 1 for 164 queued behind the 162 fix commit (single writer).
135.   Ruling (165 brief drift, pre-checked): Step 1 site is tools_read_test.go:349-373 and the limit is the constant maxPathsPorLote — test uses the constant, mutation anchor "len(req.Paths) > maxPathsPorLote"; Step 3 doctor/daemon.go does NOT read the 1<<62 offset — it calls daemon.TravaEmUso (:206) and os.ReadFile's the PID (:216) — the offset test keeps its purpose (byte 0 must stay readable while the lock is held) and adds the doctor-read assertion; the helper-process pattern already exists in trava_test.go (lancarAjudante, varAjudante, TestAjudanteSeguraTrava, package daemon internal) — reuse it, do not build a second one; Step 4 AliasCollisions() is a method on *Index (query.go:158) counting aliases with >1 paths; Step 5's "force ReadOnly=false in encode" mutation is already killed by ipc_test.go:184-205 — the live mutation is `want := HandshakeConfig{ReadOnly: readOnly` -> `ReadOnly: false` in DialAndHandshake; Step 6 test_orphans.ps1 is run by the orchestrator, not the implementer — cost if wrong: an extra fix round.
137.   Ruling: orchestrator fixed NB1-NB4 directly in a7ee204 (text only, no code; check_doc_refs [OK]) instead of a second fix round — impl-165 is the active writer and a report/CLAUDE.md edit does not touch its files — cost: no reviewer eyes on 17 changed lines of prose.
141.   Ruling: NB1(b) and NB2 ride on the next writer dispatch (batch 166-168) as a separate commit; NB1(a) — the product fix (drain hostParaDaemon non-blockingly inside the ctx.Done branch) — is OUT OF SCOPE for this plan, goes to the final out-of-scope list for the owner; NB3 keep as is (pre-existing, verify.ps1 step 3 exposes the skip count) — cost if wrong: one comment and one channel of prose land under a batch commit instead of their own review.
144. - 2026-09-06 02:01 Ruling (Task 169 normalizeKey, pre-checked): index/chave.go does NOT delegate to text (chaveDeCaminho = ToLower(text.ParaNFC(ToSlash)) inline) and vault.CanonicalPath is not NFC-normalized (no ParaNFC under internal/vault), so writer's ToLower-only lock key can give NFC and NFD spellings of one file two locks. Decision: add text.ChaveDeCaminho(string) string holding that formula; index.chaveDeCaminho delegates to it; writer.normalizeKey calls it; new production edge writer -> text, justified by "uma conta por regra", CLAUDE.md graph block updated in the same commit (writer -> parser, text, vault). chaveDeNomeDeArquivo and aliasKey stay in index (different rules). Cost if wrong: one extra edge from writer to a leaf; reversible in one commit.
145. - Ruling: Tasks 166 and 167 may land in ONE commit (message lists both scopes; separate reports; 168 separate) - the implementer had already mixed both in the tree and hand-splitting hunks across server.go/persist.go risks more than it buys - cost if wrong: coarser history, a revert of 166 drags 167 with it.
151. - 2026-09-06 06:00 Ruling: item 3 of Task 169 stays narrower than the brief (ix.tags as the flat source changes the tool answer - reviewer confirmed with data) - cost if wrong: TagListHierarquico keeps a per-note walk that the brief hoped to drop.
153. - 2026-09-06 06:33 Task 170 implemented: 8e0670c (removeLocked reverse map, -70.64% sec/op p=0.001, B/op +8.33%), 6ec12da (RewriteLinks one pass, -96.00% sec/op). Candidates 2, 4, 5 not entered (no sec/op gain). Ruling: candidate 4 stays out despite B/op -0.27% p=0.001 - Task 82 precedent, two exported methods with a silent precondition for a memory delta below noise - cost if wrong: 0.3% allocs on the cached broad-term path. Review dispatched to rev-170.
156. Task 171 BLOCKED at step 6: verify.ps1 12/14, golangci-lint SA1019 on the six callers of the writer forwarders (staticcheck is on). Ruling: forwarders keep the forwarding prose but drop the `// Deprecated:` marker ("Encaminhador transitorio para vault.X; a Task 172 migra os chamadores e o remove.") — PR1 must not touch callers, `//nolint` in six sites and a lint exclusion both outlive the one-task window, merging 171+172 defeats the move/migrate split — cost if wrong: one task without tooling strike-through on five call sites, removed by Task 172 anyway.
157. Task 171: complete (4d7f397). Review review-171.md: spec OK, quality OK, 0 blocking. Ruling: N2 (cache temp-name literals in index/persist.go:114 and search/persist.go:69), N4 (forwarder surface is 8 lines incl. servico.go:73 SweepResult) and N5 (ReplaceFile docstring must state the callback contract: do not close/rename the file, flush buffers) are carried into the Task 172 dispatch — 172 already rewrites both persist files and writes the two callbacks — cost if wrong: 172 grows by one docstring sentence. N1 (ARCHITECTURE.md §5.5 says 3 retries/50 ms, code does 10/10 ms, and omits dir fsync + Chmod) and N3 (ESTRUTURA.md lists vault/ignore.go and writer/writer.go, neither exists) are pre-existing doc drift: parked for the final doc pass after Task 181, not folded into a code task.
159. Task 172 implemented (ae619cf); review-172.md: spec approved with reservation, quality good, 1 blocking (N1 docs still describe the deleted writer/atomic.go). Fix round 1 -> impl-172: N1, N2, N3, N5 as written. Ruling on N4 (cache files now born 0644 instead of 0600 via ReplaceFile): accepted, documented in ESTADO.md's cache-format section, no signature change — the cache dir has been created 0755 under os.UserCacheDir all along, so the file mode follows the directory's posture; notes are 0644 for the same reason — cost if wrong: on a multi-user Unix host another local user can read cache contents (paths, tokens), which a later task can close by giving ReplaceFile a mode parameter. Process deviation noted: impl-172 ran verify.ps1 in background despite the foreground rule; result accepted (last line pasted, exit 0).
162. Task 173 BLOCKED at step 3: internal/service/frontmatter_err_test.go:55 asserts svc.index.(*index.Index) to reach Replace. Ruling: not a fake, a convenience assertion the change makes invalid — replace with `ix := svc.index`, drop the now-unused import, add the test file to the commit paths; the brief's "no test edited" rule guards against fakes, and the pre-check found none — cost if wrong: none observable, the test keeps the same assertions.
165. Task 174 impl commit eac13c9 (DONE_WITH_CONCERNS, 8 concerns; verify 14/14 after an OPERACAO.md:1525 check_doc_refs dispensation). Ruling: concerns 1-3 accepted (verbatim bodies beat the brief's illustrative code; no forward reference to Task 177 in doc.go; comment names serveEmProcesso) - move-not-rewrite is the binding rule - cost if wrong: two log strings differ from the brief's wording. Concerns 5 (stale .go comments citing serve.go) and 6 (docs/wiki) parked for the final doc pass. Review package review-7119645..eac13c9.diff; rev-174 (Opus) dispatched read-only. Orphans gate launched detached 09:35 (pwsh PID 3840, log orphans-174.log). Bench daemon PID 51516 on vault_5000 left from impl-174's measure - not mine, left to idle out.
166. Task 174 review: approved with should-fix N1-N5 (all docs), nits N6-N8 parked. Fix round 1 by impl-174 -> 33cff51 (4 files, 11 lines). Ruling: orchestrator re-reviewed the 11-line doc diff itself against N1-N5 instead of dispatching a re-reviewer - each finding is a named sentence and the diff shows each replaced - cost if wrong: a stale doc sentence survives to the final doc pass. Orphans gate run 1 (09:35-09:48, log orphans-174.log): stdin-eof 100/100 OK, parent-death 100/100 OK; signal and daemon-idle REFUSED to run because impl-174's fix edited doc.go after bin/gobsidian.exe was built (freshness check). Rebuilt at 33cff51, signal + daemon-idle rerun detached 09:49 (pwsh PID 26048, log orphans-174b.log). Lesson: no edits to the tree while the gate runs, not even comments.
177. - Ruling: N1 parked, no extraction now -- three copies is the codebase's file-per-operation convention and an `abrirCLI` would be the fourth helper-shaped thing this plan adds; revisit if a fourth CLI reader appears -- cost if wrong: three copies of one block drift.
178. - Ruling: N2 left as written -- the sentence corrects itself in the same clause; folded into the final doc pass list instead of a fix round -- cost if wrong: one reader misreads doctor as having --cache-dir.
182. - Ruling: accept VigiarHost(parent, stdin, log) (context.Context, *Vigia) instead of the brief's Vigia.Ctx field -- contextcheck rejects ctx stored in a struct and lifecycle.New already returns (ctx, *Lifecycle); a //nolint would silence a legitimate warning -- cost if wrong: one extra return value at three call sites.
186. - Ruling: N2 parked to the final doc pass (wiki carries source_commit and the brief did not ask for it) -- cost if wrong: one stale wiki page until the pass.
195. - Ruling: N2 parked -- the two missing rows had no min/max and no false claim; the table is a report artifact, not a deliverable -- cost if wrong: none in code.
200. - Task 180 commit 1: 937e54c (bench). Ruling: parser NFD truncation (ext_tag.go:36 tagNameChar lacks unicode.Mn; inline #Ação in NFD indexes as "ac") stays OUT of Task 180 — not in the brief's Files, would mix a parser behavior change with goldens/parity into an index-key task; NFD fixture moved to frontmatter, follow-up recorded in SUGESTOES.md — cost if wrong: one more small task later.
203. - Ruling (N1): doc follows code — `tag_list.prefix` is a string prefix over the folded key (autocomplete), distinct from the segment-hierarchical `tags` filter; segment match would make the schema's own `civil/` example return nothing and would undo Task 169's deliberate golden — cost if wrong: one doc sentence plus a golden regen later.
204. - Ruling (N7): accepted as-is, same property porFrontmatter already has; N8 → owner list / final doc pass.
206. - Task 180 fix round 1: aa8ec0b (TOOLS.md prefix contract rewritten to string-prefix + paragraph separating it from the segment `tags` filter; mcpsrv jsonschema aligned; TestTagListDobraOPrefixoNosDoisRamos + accented-prefix check in tag_chave_test; N4 precedence fixed; ESTADO B20 pointer; note_list.tags spelling note; CLI index count "não medido"). Orchestrator re-reviewed the 181-line diff (doc+test+one jsonschema string): three N2 mutations and the N4 mutation exit 0, NFD literals verified as U+0327/U+0303 bytes, verify -SkipCross -SkipNet green. Ruling: scoped re-review by orchestrator instead of a reviewer dispatch — small diff, evidence pasted — cost if wrong: a reviewer catches it in the final whole-branch pass. Orphans gate not run for 180: no lifecycle/daemon/cmd change.
208. - Task 181 dispatched to impl-181 (Sonnet), base 833906f. Rulings carried: (1) brief's `escritor{w: &buf}` cannot compile — `w` is `*bufio.Writer`; use bufio.NewWriter + Flush — cost if wrong: none, compile error would surface; (2) ESTADO.md has no "dívida 1.12" entry (segregacao numbering) — record coverage under `## Formato de cache` — cost if wrong: one paragraph moved in the final doc pass; (3) a codec behavior the brief's tests assume and the code lacks is a SUGESTOES finding, not a production fix.
211. - Task 181 fix round 1: 7d4d684 ([]any(nil) case, valSliceNil mutation exit 0; errors.Is sentinel on every truncated prefix; ESTADO coverage lines re-measured: escritor.value 92.1->97.4, leitor.value 91.7->94.4, package 89.8%). Orchestrator re-reviewed the 23-line diff. Ruling: scoped re-review by orchestrator — two-line test additions with mutation output pasted — cost if wrong: final review catches it.
216. - Ruling: F1 fix as `fix(cli)`; orphans gate NOT required -- doctor is off the serve/daemon shutdown path -- cost if wrong: an orphan scenario changes without the gate noticing (none plausible from a flag removal).
217. - Ruling: F2 fix as `fix(mcpsrv)` with one small served-schema test on the `subtags` substring -- description is the only prose the host receives (TOOLS.md:802) -- cost if wrong: a test tied to doc prose breaks on the next rewording.
218. - Ruling: N1 comment follows code (any `.lock`), predicate unchanged -- runtime dir is product-only -- cost if wrong: none observable.
219. - Ruling: N2 (tag_list min_count default at the MCP edge) PARKED -- no observable difference; owner list -- cost if wrong: one rule applied to 3 of 4 tools.
220. - Ruling: N3 (wall-clock assertion in TestQ3PerformanceMeasurement) PARKED, owner list -- cost if wrong: one flaky test under load.
221. - Ruling: N4 (`var cacheMagic`) PARKED, keep -- test pins it -- cost if wrong: a writable format id nobody writes.
222. - Ruling: N5 comment gets the cache-dir-inside-vault caveat, no code change -- default CacheDir is outside the vault -- cost if wrong: none.

## Parqueado para o dono (fora do escopo deste plano)

Da revisão final:
- rev-final N2 — `tag_list.min_count` ainda tem o padrão (`minCount := 1`) na borda MCP (`internal/mcpsrv/tools_read.go:296`), contra a regra da Task 168 (padrão no service). Sem diferença observável hoje.
- rev-final N3 — `TestQ3PerformanceMeasurement` (`internal/search/persist_test.go`) afirma `loadDur >= rebuildDur` sob `-race`; margem ~4x; único teste da branch cujo veredito depende de escalonamento.
- rev-final N4 — `cacheMagic` virou `var` (conta única com `CacheFormatVersion`); alternativa seria `const` + teste comparando com `Sprintf`.
- rev-final N5 — `--cache-dir` dentro do cofre poria o temporário de `AbrirIndice` ao alcance de `SweepStaleTempFiles` (comentário adicionado em `montar.go`; sem guarda em código).
- impl-final A2 — `docs/ESTRUTURA.md`: `scripts/` tem 11 scripts a mais do que a árvore lista e `.github/workflows/release.yml` falta.

Achados abertos em `docs/SUGESTOES.md`: B19 (`heading_level` não aplicado; `PatchNoteRequest.HeadingLevel` morto), B20 (parser trunca `#Ação` em NFD — `ext_tag.go:36` sem `unicode.Mn`), B21 (`default` inalcançável em `leitor.value`).

Carregados de Tasks anteriores (todos no ledger com a Task de origem):
- `cmd/gobsidian/ponte.go` corrida latente no drain de `hostParaDaemon` (165 NB1(a)).
- `lancarAjudante` usa `t.Skipf`; `abrirExclusivo` Skip vs Fatal; `getVault5000Path` Skip quando `%TEMP%\vault_5000` falta (verify.ps1 conta o skip em voz alta).
- `docs/TOOLS.md:403` prosa de `default`; `vault_stats` agora documenta só os campos reais (A7).
- Task 151: `write.go:712` Stat fora do lock no ramo trash → INTERNAL em vez de NOTE_NOT_FOUND no perdedor de uma corrida; sites `Errorf(CodeInternal)` em write.go.
- daemon/doctor: flags possivelmente ignoradas além de `--debounce-ms` — não auditadas.
- `daemon.go EhDesconexaoLimpa` sem cobertura direta.
- Corpus rnf5000 1,4 MB contra os 50 MB do PRD; RNF-02 sem teto; seed do gerador não verificada; universo de paridade.
- `config_test.go:203-208` return silencioso; `limites_enums_test` escreve 501 arquivos por execução; dois sleeps no watcher.
- Mensagens de commit d12f84f/af93306 com contagens erradas (corrigidas nos relatórios, não em git).
- Taxa de falso-positivo por mtime; `graph.go` ordenação de arestas instável; `resolve.go` alias aceita anexos; custo NFC na chave de lock.
- Commits 06c8640..d7a11e8 não sobem (`go build` falha no meio da Task 166-168); Task 170 baseline não reproduzível; pergunta sobre reverter impl-166-168.
- Cache files 0644 em Unix (172 N4); benchmarks `...ComFsync` redundantes; custo de fsync em `SaveInvertedCacheReal`; `verify.ps1` etapa 2 sem `-count=1`; lacuna M4 da Task 175; Task 179 tamanho compacto 1,8% abaixo do esperado.
- `parser.dedupeTags` (`ast.go:158`) é uma segunda conta de "mesma tag" (ToLower sem NFC) — caminho: mover a conta para `internal/text`.
- Golden de `tag_list` (Task 169) não exercita dobra; só `tags_contrato_test` cobre.
- `porTag`/`porFrontmatter` retrato fora do laço (180 N7).
- `valSliceNil` possivelmente inalcançável a partir de YAML real.
- `scripts/audit_reports.ps1`: 14 achados pré-existentes no ledger antigo (M0–M6) — SHA-NAO-CONFERE ×3, RELATORIO-AUSENTE ×9, SHA-FANTASMA ×1.
- Processos `bin\gobsidian.exe daemon` sobrando de gates anteriores — conferir `tasklist` antes do próximo gate de órfãos.
- ESTRUTURA.md:229-230 grafo antigo (mcpsrv → search) — conferir se A2 cobriu.

## Estado da árvore

Não commitado, do dono: `test-vault/`, `.claude/skills/`, `Resume-Claude.ps1`,
e o ledger `.superpowers/sdd/2026-07-25-gobsidian-v01/progress.md` (+ ~230
linhas deste plano, incluindo as migradas do ponteiro). Não commitado,
descartável: `final-diff-*.diff`, `orphans-*.log.err`, `flags-grep.txt`.
